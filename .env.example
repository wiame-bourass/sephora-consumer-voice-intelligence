# Copy this file to .env and fill the values.
# The endpoint must accept a chat-completions-compatible JSON payload.
LLM_ENDPOINT_URL=
LLM_API_KEY=
LLM_MODEL=

# Optional custom Authorization prefix; default is "Bearer".
LLM_AUTH_SCHEME=Bearer
